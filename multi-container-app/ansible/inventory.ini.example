# Kopyala: cp inventory.ini.example inventory.ini  (gerçek dosya gitignore'da)
# IP'yi `terraform output ec2_public_ip` ile al
[todo_app]
<EC2_PUBLIC_IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/todo-app-ec2-key
